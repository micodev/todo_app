import 'package:flutter/material.dart';

class ErrorTodoWidget extends StatelessWidget {
  const ErrorTodoWidget(String errorMessage, {super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
